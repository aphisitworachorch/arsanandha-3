version: '3.7'

networks:
  frontend-service:
    driver: bridge

services:
  frontend:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - frontend-service
    restart: always